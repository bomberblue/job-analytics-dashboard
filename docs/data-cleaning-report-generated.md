# Data Cleaning Report - SGJobData

Generated by `notebooks/data_cleaning.ipynb`. Source: `../data/raw/SGJobData.csv`. Every figure is computed at run time from the data itself.

| | rows | cols | memory (MB) | NaN cells |
| --- | ---: | ---: | ---: | ---: |
| raw | 1,048,585 | 22 | 401.7 | 1,092,453 |
| clean | 1,044,587 | 27 | 162.7 | 28,731 |

**3,998 rows removed (0.38%). Memory -59%. Cells imputed: 0.**

### Step ledger

| step | rows | cols | mem MB |
| --- | --- | --- | --- |
| Step 2 - remove ghost rows | 1,048,585 -> 1,044,597 | 22 -> 22 | 402 -> 409 |
| Step 3 - remove synthetic test rows | 1,044,597 -> 1,044,587 | 22 -> 22 | 409 -> 409 |
| Step 4 - drop empty and zero-variance columns | 1,044,587 -> 1,044,587 | 22 -> 19 | 409 -> 377 |
| Step 5 - parse date columns | 1,044,587 -> 1,044,587 | 19 -> 19 | 377 -> 345 |
| Step 6 - normalise categories JSON | 1,044,587 -> 1,044,587 | 19 -> 20 | 345 -> 291 |
| Step 7 - fix salary sentinels and outliers | 1,044,587 -> 1,044,587 | 20 -> 21 | 291 -> 287 |
| Step 7b - cap impossible experience values | 1,044,587 -> 1,044,587 | 21 -> 21 | 287 -> 282 |
| Step 8 - normalise title and company text | 1,044,587 -> 1,044,587 | 21 -> 21 | 282 -> 282 |
| Step 9 - filling decisions and derived columns | 1,044,587 -> 1,044,587 | 21 -> 24 | 282 -> 297 |
| Step 10 - flag duplicates | 1,044,587 -> 1,044,587 | 24 -> 27 | 297 -> 304 |
| Step 11 - categorical dtypes and integer downcast | 1,044,587 -> 1,044,587 | 27 -> 27 | 304 -> 163 |
| Step 13 - feature enrichment | 1,044,587 -> 1,044,587 | 27 -> 33 | 163 -> 197 |
| Step 14 - prune low-value derived columns | 1,044,587 -> 1,044,587 | 33 -> 31 | 197 -> 186 |

### Parameters

| parameter | value | meaning |
| --- | --- | --- |
| `SALARY_FLOOR` | 500 | monthly SGD at or below which a salary is a placeholder - **nulled** |
| `SALARY_CEILING` | 100,000 | monthly SGD above which a salary is a statistical outlier - **flagged, not nulled** |
| `INTERN_STIPEND_FLOOR` | 300 | monthly SGD below which even an internship stipend is implausible |
| `EXPERIENCE_MAX` | 100 | years above which the value is impossible |
| `SYNTHETIC_ID_RE` | `^RANDOM_JOB_` | job-ID pattern marking generated test rows |

---

## 1. Column type conversions - and where data would be lost

### 1.1 Dates: `str` -> `datetime64[us]`  *(lossless, asserted)*

| column | from | to | unparseable | round_trip_identical | min | max |
| --- | --- | --- | --- | --- | --- | --- |
| metadata_newPostingDate | str | datetime64[us] | 0 | True | 2023-03-28 | 2024-05-29 |
| metadata_originalPostingDate | str | datetime64[us] | 0 | True | 2022-10-03 | 2024-05-29 |
| metadata_expiryDate | str | datetime64[us] | 0 | True | 2023-04-04 | 2024-06-28 |

**Justification.** As strings these columns cannot do date arithmetic, resampling, or `.dt` access - every time-series view in the dashboard needs real datetimes. **No data loss:** the notebook refuses the conversion unless `strftime` round-trips to the original string on every row, which it does. No timezone is applied, because the source has no time component and localising would imply precision that does not exist.

Cross-field logic also holds: `originalPostingDate > newPostingDate` in **0** rows, `expiryDate <= newPostingDate` in **0** rows. Median lifespan 30 days.

### 1.2 `categories` JSON -> bridge table  *(lossless as done; lossy if shortcut)*

- 43 distinct categories, 1,767,785 assignments over 1,044,587 postings
- categories per posting: 1 -> 654,951 postings, 2 -> 208,746 postings, 3 -> 85,461 postings, 4 -> 38,186 postings, 5 -> 57,243 postings
- 37.3% of postings carry more than one category

| category | postings |
| --- | --- |
| Information Technology | 140,865 |
| Engineering | 136,371 |
| Admin / Secretarial | 117,849 |
| Customer Service | 111,780 |
| Others | 106,097 |
| Sales / Retail | 105,063 |
| Building and Construction | 84,031 |
| Accounting / Auditing / Taxation | 78,647 |
| F&B | 73,731 |
| Logistics / Supply Chain | 69,193 |

**Justification.** A JSON string cannot be filtered on - "all IT jobs" would need a substring match that also catches the label inside unrelated text. The many-to-many relationship is preserved in `job_category.parquet` (1,767,785 rows). **This is where loss would occur if shortcut:** keeping only the first category would discard 723,198 assignments and systematically understate every secondary category. `primary_category` is kept on the wide table as a convenience only.

### 1.3 Categorical and integer conversions  *(lossless, range-asserted)*

| column | from | to | observed_range | headroom | safe |
| --- | --- | --- | --- | --- | --- |
| minimumYearsExperience | Int16 | Int8 | 0 - 88 | 39 | True |
| metadata_repostCount | int64 | int8 | 0 - 2 | 125 | True |
| numberOfVacancies | int64 | int16 | 1 - 999 | 31,768 | True |
| metadata_totalNumberOfView | int64 | int32 | 0 - 8190 | 2,147,475,457 | True |
| metadata_totalNumberJobApplication | int64 | int32 | 0 - 1342 | 2,147,482,305 | True |
| listing_days | int16 | int16 | 1 - 30 | 32,737 | True |
| n_categories | int8 | int8 | 1 - 5 | 122 | True |
| employmentTypes | str | category | 8 distinct | 16.8 MB saved | True |
| positionLevels | str | category | 9 distinct | 20.7 MB saved | True |
| status_jobStatus | str | category | 3 distinct | 11.9 MB saved | True |
| postedCompany_name | str | category | 53,150 distinct | 31.4 MB saved | True |
| primary_category | str | category | 43 distinct | 26.9 MB saved | True |
| source | str | category | 2 distinct | 10.4 MB saved | True |

**Justification.** Eight distinct employment types over a million rows is the textbook case for `category`: the string is stored once and each row holds a small integer code. Every integer downcast is asserted against the observed range before being applied, so the loop fails loudly rather than wrapping silently. The two `metadata_total*` counters are given `int32` rather than the tighter `int16` their current range permits - `int16` leaves only ~24k headroom and one viral posting in a future extract would overflow it.

### 1.4 `average_salary` -> `float32`: the conversion whose answer depends on when you ask

| measured at | `.5` values above $8,388,608 | max round-trip error |
| --- | ---: | ---: |
| raw file | 1 | **0.5** |
| after synthetic rows removed | 0 | 0.0 |
| after the Step 7 salary fix | 0 | 0.0 |

**On the raw file the cast is lossy** - error 0.5, and it traces to a single row. float32 has a 24-bit mantissa, so above $8,388,608 it cannot represent a `.5` at all, and the raw file holds exactly 1 such value - a synthetic posting at $10,593,120.5. That single row is the whole of the error. The `.5` fractions themselves are ordinary: 3,838 surviving rows carry one (odd min+max) and every one round-trips exactly, because they sit below the limit. Remove the synthetic rows and the cast becomes lossless.

The point is not that float32 is unusable - it is that "is this conversion lossless?" has no answer independent of the cleaning order. Profiling dtypes on a dirty frame gives a different verdict than profiling the same frame after junk-row removal, which is why every dtype decision in this notebook is made last.

It is **kept as `Float64` regardless**: the saving is only a few MB, and float32 would silently re-break the moment a future extract reintroduces large values. `average_salary` is also fully derived from `salary_minimum`/`salary_maximum`, so it is recomputed after the salary fixes rather than trusted - a stored derived column drifts out of sync with its inputs.

### 1.5 Columns dropped as dead

| column | reason |
| --- | --- |
| `occupationId` | 100% NaN, 0 distinct values - empty column |
| `salary_type` | single value `Monthly` on every row - zero variance |
| `status_id` | single value `0` on every row - zero variance |

**Justification.** A constant column cannot correlate with anything and cannot be filtered on meaningfully. `salary_type` being constant is itself information - *all salaries are monthly SGD* - and belongs in this report rather than repeated a million times in the frame. `metadata_isPostedOnBehalf` is retained despite its 94/6 split: low variance is not zero variance, and it flags recruiter-posted listings, which matters for the duplicate analysis in section 5.

---

## 2. Ghost rows removed

**3,988 rows (0.38% of the file) - all removed.**

| evidence | value |
| --- | --- |
| rows where all 11 text columns are NaN | 3,988 |
| rows with a *partial* NaN pattern | **0** |
| observed NaN-count-per-row values | [0, 11] |
| sum of abs() over every numeric column on these rows | **0.0** |
| rows with 0 vacancies outside this group | 0 |
| index span | 197,478 - 606,701 |

**Justification for deletion rather than imputation.** The per-row NaN count is strictly bimodal - a row is either fully populated or fully blank, never in between - and every numeric field on the blank rows is exactly zero. There is nothing to impute *from*: no title, no company, no ID, no dates. Every real posting has at least one vacancy; a record with zero vacancies, zero salary and no identifier is not a job. Imputing would fabricate 3,988 synthetic postings and inflate every count on the dashboard. At 0.38% of the data, dropping costs nothing statistically.

**Ordering trap.** `occupationId` is 100% NaN, so on the raw frame `df.dropna()` returns an **empty** DataFrame. The notebook uses an explicit all-text-NaN mask instead, which states the intent and cannot misfire.

### Synthetic test rows: 10 more removed

Rows matching `^RANDOM_JOB_` are generated test data - the IDs embed a generation timestamp and salaries reach $23,712,119/month. Removed by ID pattern, not by index, so the filter survives a reload. ID prefixes present in the raw file: {'MCF': 1044463, 'ATS': 124, 'RANDOM_JOB_': 10}. The `ATS-` rows are legitimate (real companies, sane salaries) and are retained, tagged via the new `source` column: {'MCF': 1044463, 'ATS': 124}.

---

## 3. Data fixing, per column

### 3.1 `salary_minimum` / `salary_maximum` / `average_salary`

**Before**

| index | salary_minimum | salary_maximum | average_salary |
| --- | --- | --- | --- |
| count | 1,044,587 | 1,044,587 | 1,044,587 |
| mean | 3,828.2 | 5,630.8 | 4,729.5 |
| std | 3,104.3 | 27,096 | 13,882.9 |
| min | 1 | 1 | 1 |
| 1% | 500 | 1,000 | 800 |
| 50% | 3,000 | 4,500 | 3,800 |
| 99% | 13,000 | 20,000 | 16,750 |
| 99.9% | 20,000 | 35,000 | 27,470.7 |
| max | 350,000 | 25,330,000 | 12,666,400 |

**After**

| index | salary_minimum | salary_maximum | average_salary |
| --- | --- | --- | --- |
| count | 1,035,010 | 1,035,010 | 1,035,010 |
| mean | 3,862.9 | 5,673.5 | 4,768.2 |
| std | 3,097.4 | 27,193.5 | 13,929.4 |
| min | 300 | 400 | 350 |
| 1% | 1,000 | 1,400 | 1,150 |
| 50% | 3,000 | 4,500 | 3,850 |
| 99% | 13,000 | 20,000 | 16,875 |
| 99.9% | 20,000 | 35,000 | 27,249.1 |
| max | 350,000 | 25,330,000 | 12,666,400 |

**The floor and ceiling are handled differently on purpose** - not two symmetric guard rails, but two different kinds of problem:

| defect | rows | treatment |
| --- | ---: | --- |
| `salary_minimum < 500` (undisclosed placeholder) | 9,581 | **null** the pair |
| `salary_maximum < 500` (undisclosed placeholder) | 7,124 | **null** the pair |
| of which exactly `$1 - $1` | 1,804 | **null** the pair |
| low minimum but plausible maximum (e.g. `$1 - $600`) | 2,457 | **null** the pair |
| internship, both bounds in `[300, 500)` | 4 | **keep**, flag `low_stipend` |
| `salary_maximum > 100,000` (statistical outlier) | 269 | **keep, do not null** - flag `outlier` |
| `salary_minimum > salary_maximum` | 0 | none needed |

**Why the asymmetry.** `$1/month` and a `$10`-`$15` hourly rate typed into a monthly-only field are not real numbers at *any* resolution - there is nothing to preserve, so the floor defect is nulled. A `$100,000+`/month posting is different: it might be a genuine C-suite salary or it might be a missing decimal, and which one is true depends on the question being asked of the data - a mean/std view wants it excluded, a "highest-paid roles" or fraud-detection view wants to see it. That is an analysis-stage judgment, not a cleaning-stage fact, so the raw value is left untouched and only a `salary_flag == 'outlier'` marker is added. Whether to filter on it is left to whoever runs the query.

**The pair is the unit of validity for the floor check.** A quoted salary is a *range*, so if either bound is a placeholder the whole range is untrustworthy. Nulling only `salary_maximum` would leave 2,457 rows like `$1 - $600` behind, whose average of $300.50 sits below the very floor the rule is meant to enforce. Both bounds are therefore nulled together, and a post-condition asserts they are never nulled apart.

**Justification for the floor.** $1/month is not a wage - it is what a poster enters to satisfy a required field. Singapore has no statutory minimum wage, so there is no bright line; $500 is a conservative "cannot be a real monthly wage" threshold well below the Progressive Wage Model floors (~$1,400-1,600). Note the low group skews part-time and contract ({'Part Time': 3461, 'Contract': 1947, 'Temporary': 1279, 'Permanent': 1136}), so some are hourly rates forced into a monthly-only field - mis-unit rather than undisclosed, but both must be excluded from salary aggregates.

**Internship carve-out.** A flat $500 floor risked deleting genuine low stipends, so it was checked before being applied rather than assumed. Internship salaries in this file have a median of $1,200 and a 5th percentile of $800 - almost none are legitimately this low - but 4 rows tagged `Internship/Attachment` with both bounds in `[300, 500)` are real stipends, not placeholders. They are kept and marked `low_stipend` instead of `undisclosed`, visible to any analysis that wants to exclude them, invisible to one that does not - the same flag-don't-null principle used for the ceiling, applied at the low end.

**Why not the textbook IQR rule** for a ceiling, if one were applied at cleaning time at all. Q1=$2,900, Q3=$5,500, so even the lenient 3xIQR upper fence sits at $13,300 and would catch 24,520 rows - tens of thousands of legitimate senior roles. Whatever threshold an analysis chooses to filter `salary_flag == 'outlier'` on, it should not be the IQR rule.

Total nulled: **9,577** rows (floor only). Remaining salary coverage: **99.08%** - higher than if the ceiling were also nulled, because those 269 rows are still present, just flagged. `average_salary` is recomputed from the cleaned inputs rather than inherited, which means it **still contains the flagged outliers** (max $25,330,000) until an analysis filters them out - that is the point, not an oversight. A single reason code is built rather than a bare NaN: `salary_flag` (`{'ok': 1034742, 'undisclosed': 9577, 'outlier': 264, 'low_stipend': 4}`) records *why* a value is missing or suspect - `undisclosed`, `outlier`, or the retained `low_stipend` carve-out - so the reason is never lost along with the value. Filter with `salary_max.notna()`, or on the flag when the reason matters.

### 3.2 `minimumYearsExperience`

Observed max was **88** years; tail values {59: 1, 61: 1, 62: 1, 76: 1, 87: 2, 88: 1}. Values above 100 are physically impossible and were set to NaN - **0 rows**, too few to move any aggregate. These are poster-side typos, not a pipeline defect, which is why the column is capped rather than rebuilt. `0` is left untouched (see section 4).

### 3.3 `title` and `postedCompany_name`

| fix | before | after |
| --- | ---: | ---: |
| rows changed by the `title` repair | 21,934 | 0 |
| ...of those, carrying a zero-width character | 184 | 0 |
| rows changed by the `postedCompany_name` repair | 9,616 | 0 |
| distinct titles | 377,084 | 364,753 (case-folded) |
| distinct companies | 53,151 | 53,150 (`postedCompany_name`, repaired in place) |

**Justification.** Three operations get lumped together as "normalisation" and are treated differently here. A **repair** - stripping whitespace, collapsing internal runs, dropping zero-width characters - goes **in place**: `"  Chef "` carries no information `"Chef"` lacks, so there is nothing to preserve, and the changed-row counts above are what make the mutation auditable. A **re-key** - case-folding - gets its **own column**, because it destroys a display form (`IT` vs `it`, `PhD`, `C++`) that the dashboard needs.

**Zero-width characters are part of the repair**, because `\s` does not match them. U+200B, the BOM and the word joiner render as nothing yet split otherwise-identical titles into separate values - `ACCOUNTS ASSISTANT` prefixed with a zero-width space counts separately from `ACCOUNTS ASSISTANT` in every chart. The joiners U+200C/U+200D are excluded from that class deliberately: U+200D is what holds an emoji sequence together, and 75 rows (52 distinct titles) use one, so stripping it would corrupt content rather than clean it.

`title` gets the repair in place. The fold is **not stored at all** - not as an overwrite, because it destroys the display form, and not as a second column, because `title.str.lower()` rebuilds it in one line from a value already on disk. It is computed as a local key where it is used (section 5) and discarded. It earns its place there: it collapses **12,331** distinct titles - without it "Software Engineer" and "software engineer" count as different roles, and the duplicate check misses 322 rows - and what survives into the saved data is `dup_group_id`, the result of the folded grouping.

`postedCompany_name` gets the repair **plus an in-place `.upper()`** - the one place this pipeline knowingly bends the rule above. The source is uppercase for all but a single entry, so the fold merges exactly one pair: `Church of Our Saviour` into `CHURCH OF OUR SAVIOUR`. That is a real display form being destroyed, so strictly it should earn its own column. It does not get one because an earlier `company_normalised` cost 6.1 MB and a second name for one concept in order to merge that single pair; normalising a lone outlier to the house style of the other 53,149 is the better trade, and it leaves `postedCompany_name` as the join key. Whitespace alone merges nothing here - all 9,616 defects are internal double spaces inside names that stay distinct.

Legal-suffix variation (`PTE. LTD.` vs `PTE LTD`) is left alone deliberately: that is entity resolution, not cleaning, and needs its own reviewed pass.

### 3.4 Columns needing no fix

`categories` parses cleanly on every row with 0 empty arrays. `numberOfVacancies` spans 1-999 with no zeros; 999 looks like a UI cap rather than a true count, which is worth a footnote on any "total vacancies" headline but is not a defect to repair. `status_jobStatus`, `employmentTypes` and `positionLevels` have small clean value sets.

---

## 4. Data filling, per column

**0 cells were imputed.** After ghost-row removal the frame has no explicit NaN outside the salaries this pipeline deliberately created. The filling question is really about *implicit* missingness encoded as `0`:

| index | zeros | zero_pct | decision |
| --- | --- | --- | --- |
| metadata_totalNumberJobApplication | 656,375 | 62.8 | leave - ambiguous |
| metadata_totalNumberOfView | 179,121 | 17.1 | leave - ambiguous |
| minimumYearsExperience | 114,451 | 11 | leave - real value |
| numberOfVacancies | 0 | 0 | n/a - min is 1 |
| metadata_repostCount | 1,001,862 | 95.9 | leave - real value |

### Why nothing is filled

**`metadata_totalNumberJobApplication`** - zero is genuinely bimodal: it means "nobody applied" for many listings and "the counter was not populated at scrape time" for others, and the data offers no way to separate them. Mean-filling would fabricate applications; NaN-ing all of them would discard the majority of the column. Left as `0`, flagged here, and application-rate metrics should be restricted to rows with views > 0.

**`metadata_totalNumberOfView`** - same ambiguity at a lower rate. A posting with 0 views and 0 applications is internally consistent, so view-based analysis on the non-zero subset is defensible. Guard the division: `applications / views` is undefined for these rows.

**`minimumYearsExperience`** - `0` is a real value meaning "no prior experience required". This was tested rather than assumed - the seniority mix of the zero-experience rows against the frame overall:

| positionLevels | zero_exp_% | overall_% |
| --- | --- | --- |
| Fresh/entry level | 61.7 | 11.4 |
| Non-executive | 13.6 | 12.6 |
| Executive | 10 | 24.3 |
| Junior Executive | 8.2 | 16 |
| Professional | 4.5 | 10.7 |

The zeros concentrate at the junior end rather than spreading evenly across seniority, which is what a defaulted-to-zero field would look like. They are real.

**Salaries** - the ~9,577 nulled values (the floor defect only) are **not** imputed by median, group median, or regression. Salary is the dependent variable this dashboard exists to measure; filling it with a group median manufactures the very distribution being observed and tightens variance so every confidence interval comes out wrong. Exclude from aggregates and report the 99.08% coverage rate alongside every salary figure. If a downstream model cannot accept NaN, add an explicit `salary_imputed` flag so the imputation is never invisible.

The 269 ceiling outliers are a related but separate case: they are not missing, so there is nothing to fill. `salary_flag == 'outlier'` marks them and leaves the decision to exclude, cap, or report them separately to whichever analysis consumes the data - see section 3.1 for why that choice is deferred rather than made here.

### Derived columns added instead of filling

| column | definition |
| --- | --- |
| `listing_days` | expiryDate - newPostingDate |
| `is_repost` | repostCount > 0 |
| `source` | ID prefix (MCF / ATS) |
| `salary_flag` | 'ok' / 'undisclosed' / 'outlier' / 'low_stipend' - reason code from Step 7; 'outlier' rows keep their raw value (not nulled) |
| `average_salary` | (min + max) / 2, recomputed after Step 7 |
| `primary_category` | first element of the categories JSON |
| `n_categories` | length of the categories JSON |

---

## 5. Duplicate rows

| key | duplicate_rows | pct |
| --- | --- | --- |
| 1. exact duplicate row (all columns) | 0 | 0 |
| 2. duplicate primary key (jobPostId) | 0 | 0 |
| 3. identical content, any date | 369,889 | 35.41 |
| 4. identical content, same posting date | 34,878 | 3.34 |
| 5. company + title + posting date | 49,619 | 4.75 |

**No rows were dropped as duplicates.** They are flagged via `dup_group_size` and `is_same_day_dup` so the decision belongs to the analysis that consumes the data.

### Justification

**Keys 1 and 2 are clean.** 0 exact duplicate rows and 0 duplicate `metadata_jobPostId` - the primary key is sound and the loader is not double-reading anything. Any deduplication beyond this point is a judgement about business meaning, not a repair.

**Key 3 (369,889 rows) is serial reposting, not duplication.** The same role posted by the same agency on different dates with different job IDs is how recruitment agencies work. Each is a real posting that really appeared on the platform. Collapsing them would erase the time dimension of hiring demand - exactly what a job-market dashboard is measuring.

**Key 4 (34,878 rows) is the only arguable case - and the evidence says keep.** These are 56,222 rows in 21,344 groups of identical same-day postings (largest group: 260). The decisive test: **16,990 of 21,344 groups have different view counts** across their members, and 5,902 have different application counts. Distinct traffic means the platform served them as separate listings that jobseekers found and viewed separately. Dropping them would discard real engagement data.

They are also concentrated: 37.8% are `isPostedOnBehalf` against a 5.9% baseline, and the top contributors are manpower agencies.

| postedCompany_name | duplicate_rows |
| --- | --- |
| RECRUITPEDIA PTE. LTD. | 16,044 |
| HD MANPOWER CONSULTANTS PTE. LTD. | 3,519 |
| 57 EMPLOYMENT AGENCY PTE. LTD. | 2,977 |
| MINDFLEX EDUCATION PTE. LTD. | 2,814 |
| ORIENTAL EMPLOYMENT PTE. LTD. | 2,394 |

**Recommended handling downstream.** Keep every row for volume and time-series work. When ranking employers or estimating distinct job openings, deduplicate at query time with `drop_duplicates('dup_group_id')` - reversible, and the choice stays visible in the analysis rather than being baked into the stored dataset. `numberOfVacancies` is tracked separately, so a company posting five identical roles is not the same as one posting with five vacancies.

`dup_group_id` stores the key-4 grouping as a group *number* rather than as the nine columns that produced it: 1,009,709 groups in 4.2 MB, against the 45.8 MB the case-folded title component alone would cost as a stored column. That fold is built as a local key and discarded - `title.str.lower()` rebuilds it in one line - while the grouping it produces is preserved exactly, including the 322 duplicate rows across 209 groups that a case-sensitive title key misses ("Air Freight officer" / "Air freight officer").

---

## 6. Feature enrichment - alignment with `src/pipeline/`

Implemented in **`src/pipeline/feature_enrichment.py`** and imported by the notebook, so the pipeline and any other consumer can use the same logic rather than reimplementing it.

`feature_enrichment(df, job_category=None)` renames **15** source columns to the names used by `src/pipeline/` and derives the feature columns `feature_engineer.py` adds, so all **18** `JOBS_SCHEMA` columns it produces are satisfied and the cleaned data can feed the existing dashboard without a translation layer. A further **5** schema columns are dead: they are excluded from the module's column list *and* never built (below). `schema_report()` from the same module backs the assertions that fail the notebook if a live column is missing or a dead one reappears.

| derived column | source | matches pipeline? |
| --- | --- | --- |
| `experience_level` | banded from `seniority_years` | yes - same thresholds |
| `salary_band` | banded from `salary_max` | yes - same thresholds |
| `skills`, `skill_count` | regex over `title` | yes - same vocabulary and boundary rule |
| `sub_sector` | 2nd category from the bridge table | **no** - pipeline leaves it NULL |
| `competitiveness_score` | p99 salary denominator | **no** - pipeline uses `max()` |

`skill_count` and `competitiveness_score` are built here and then dropped in section 7 - they are listed because the enrichment produces them, not because they reach the file.
| `job_id` | `metadata_jobPostId` | **no** - pipeline mints a fresh UUID |

### 5 `JOBS_SCHEMA` columns deliberately not materialised

These are absent from `JOBS_SCHEMA_COLUMNS` in `feature_enrichment.py`, so no caller can read the list and rebuild them; the full table definition still lives in `src/database/schema.py`.

| column | value it would hold | why it is dead |
| --- | --- | --- |
| `location` | `'Singapore'` on every row | the extract is Singapore-only - zero variance |
| `salary_currency` | `'SGD'` on every row | restates salary_type, itself constant 'Monthly' here and dropped in Step 4 |
| `description` | `''` (empty string) | the source CSV has no description field at all |
| `requirements` | `''` (empty string) | the source CSV has no requirements field at all |
| `created_at` | `Timestamp.now()` at run time | records when the pipeline ran, not a property of the posting; DatabaseManager.insert_jobs stamps it at insert time |

**Justification.** This is section 1.5's rule applied to the output side. Pruning `salary_type` for being constant and then inventing `salary_currency` two steps later would be incoherent: both encode the single fact *all salaries are monthly SGD*, which belongs in this report rather than in a million rows. `description` and `requirements` are worse than constant - the source CSV has no such fields, so they would be empty strings that read as "no requirements listed" rather than "never collected". `created_at` records when this notebook ran, not anything about the posting, and it would change the parquet's bytes on every execution with no change in the data.

**Nothing downstream breaks.** `DatabaseManager.insert_jobs` back-fills `created_at` and `salary_currency` itself and filters `columns_order` to the columns actually present, printing anything absent; `location`, `description` and `requirements` are nullable in `JOBS_SCHEMA`. Both outputs are asserted free of these columns, and of any all-blank column, before they are written.

**Two deliberate deviations**, both documented rather than silent:

1. **`job_id` carries the real `metadata_jobPostId`.** The pipeline regenerates it as a UUID at load time, which makes the processed rows impossible to join back to the raw layer or to MCF - the audit trail the raw layer exists to provide becomes unreachable. Keeping the source ID also lets `jobs_enriched.parquet` join to `job_category.parquet`.
2. **`competitiveness_score` divides by the 99th percentile, not `max()`.** With the outlier left in place (by design - see section 3.1), a `max()` denominator is dominated by it and compresses the median row's salary contribution to roughly 0.02 of the 50 points available. The p99 denominator gives a median score of **11.82** - a better-scaled version of a measure that section 7 then drops from the output entirely, for a reason no choice of denominator fixes.

### Two `feature_engineer.py` columns deliberately not reproduced

**`days_posted`** is `now() - posting_date`. That measures when the ingestion process happened, not any property of the job posting, and it would make the saved parquet change on every execution - the file's bytes shifting with no change in the data. On a 2023-24 extract it resolves to the same value for every row sharing a posting date, so it carries no information beyond `posting_date`, which is already a column. `listing_days` (`expiry_date - posting_date`) is retained instead: deterministic, and the duration an analysis would actually ask for. Anything needing days-since-posting can compute it at query time against its own reference date.

**`is_growth_role`** uses `count > median * 0.2`, which marks essentially every role in the file.

Neither is part of `JOBS_SCHEMA`. Worth noting more broadly: of everything `feature_engineer.py` computes, **only `seniority_years` actually reaches the `jobs` table** - `salary_midpoint`, `salary_band`, `skill_count`, `competitiveness_score`, `days_posted` and `is_growth_role` are all dropped by `columns_order` at load time. `salary_midpoint` and `salary_band` are kept in `jobs_enriched.parquet` anyway, because it is meant to be usable directly for analysis rather than only as a database feed; `skill_count` and `competitiveness_score` are not, for the separate reason given in section 7.

Coverage of the derived columns: `sub_sector` populated on **389,636** rows (37.3%), `skills` matched on **30,491** rows (2.9%, mean `skill_count` 0.032). The skills rate is low because **the source CSV has no description column** - which is also why `description` is a dead column above - so the skill regex only ever sees job titles. That limitation is inherited from the source data, but it means `skill_count` and anything derived from it should be read as a title-keyword signal rather than a requirements analysis.

`jobs_enriched.parquet` carries 13 provenance columns beyond the `jobs` table (`metadata_isPostedOnBehalf`, `metadata_originalPostingDate`, `status_jobStatus`, `salary_midpoint`, `n_categories`, `salary_flag`, ...) so a loader can `SELECT` just the `JOBS_SCHEMA` columns while the cleaning decisions stay inspectable - and none of the 5 dead ones.

---

## 7. Pruning low-value derived columns

Section 1.5 pruned columns that were dead on arrival. The same test is applied to the columns this pipeline *creates*, and **2** of them fail it, costing **10.4 MB**:

| column | distinct | zero_pct | MB | corr_applications | corr_views | corr_vacancies | reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| skill_count | 7 | 97.1 | 1 | -0.01 | -0.01 | -0 | keyword-matched from titles; the source has no description text |
| competitiveness_score | 3,483 | 0 | 9.4 | 0.09 | 0.06 | -0.03 | does not track its own inputs |

**`skill_count`** is a keyword match over job *titles*. `description` and `requirements` do not exist in the source - they are dead columns (section 6) - so the regex only ever sees a title, and a title is not where a posting lists its skills. The result is **97.1% zeros** across **7 distinct values**. A column that is almost entirely one value cannot separate anything, and the non-zero remainder measures whether a recruiter put "Python" in the headline, not whether the job needs Python.

**`competitiveness_score`** is meant to rank how contested a posting is. Correlated against the quantities that actually express contest, it returns **+0.09** with applications, **+0.06** with views and **-0.03** with vacancies. A score that does not track its own subject is worse than absent, because a dashboard presents it as though it does. Note this is a verdict on the *measure*, not on the p99 denominator argued for in section 6.2 - that fix improves the scaling of a quantity that still fails to correlate with anything it claims to summarise.

Both are **computed and then dropped** rather than removed from `feature_enrichment.py`: that module is shared production code, and `competitiveness_score` needs `skill_count` to build. What this step decides is only what is worth *storing*.

**`skills` is kept**, despite 97.1% of rows reading "Not Specified" across 297 distinct values and 21.7 MB - it is a `JOBS_SCHEMA` column, so dropping it would break the loader contract. It carries the same title-only limitation as `skill_count` and should be read the same way: a headline-keyword signal, not a requirements analysis. Worth revisiting when the schema next changes.

---

## Appendix - final column profile

| index | dtype | na_count | na_pct | n_unique | zeros |
| --- | --- | --- | --- | --- | --- |
| employmentTypes | category | 0 | 0 | 8 | 0 |
| metadata_expiryDate | datetime64[us] | 0 | 0 | 449 | 0 |
| metadata_isPostedOnBehalf | bool | 0 | 0 | 2 | 0 |
| metadata_jobPostId | str | 0 | 0 | 1,044,587 | 0 |
| metadata_newPostingDate | datetime64[us] | 0 | 0 | 429 | 0 |
| metadata_originalPostingDate | datetime64[us] | 0 | 0 | 603 | 0 |
| metadata_repostCount | int8 | 0 | 0 | 3 | 1,001,862 |
| metadata_totalNumberJobApplication | int32 | 0 | 0 | 369 | 656,375 |
| metadata_totalNumberOfView | int32 | 0 | 0 | 1,543 | 179,121 |
| minimumYearsExperience | Int8 | 0 | 0 | 47 | 114,451 |
| numberOfVacancies | int16 | 0 | 0 | 97 | 0 |
| positionLevels | category | 0 | 0 | 9 | 0 |
| postedCompany_name | category | 0 | 0 | 53,150 | 0 |
| salary_maximum | Int32 | 9,577 | 0.92 | 2,486 | 0 |
| salary_minimum | Int32 | 9,577 | 0.92 | 2,014 | 0 |
| status_jobStatus | category | 0 | 0 | 3 | 0 |
| title | str | 0 | 0 | 375,510 | 0 |
| average_salary | Float64 | 9,577 | 0.92 | 3,915 | 0 |
| primary_category | category | 0 | 0 | 43 | 0 |
| n_categories | int8 | 0 | 0 | 5 | 0 |
| salary_flag | category | 0 | 0 | 4 | 0 |
| listing_days | int16 | 0 | 0 | 13 | 0 |
| is_repost | bool | 0 | 0 | 2 | 0 |
| source | category | 0 | 0 | 2 | 0 |
| dup_group_id | int32 | 0 | 0 | 1,009,709 | 1 |
| dup_group_size | int16 | 0 | 0 | 56 | 0 |
| is_same_day_dup | bool | 0 | 0 | 2 | 0 |

### Outputs

- `data/processed/jobs_cleaned.parquet` - 1,044,587 rows
- `data/processed/job_category.parquet` - 1,767,785 rows
- `data/processed/jobs_enriched.parquet` - 1,044,587 rows